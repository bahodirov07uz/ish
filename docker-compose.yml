version: "3.9"

services:
  web:
    build: .
    restart: always
    env_file: .env
    # Gunicorn orqali ishga tushirish
    command: uv run gunicorn config.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"